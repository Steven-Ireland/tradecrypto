buildscript {
    repositories {
        maven {
            url "https://plugins.gradle.org/m2/"
        }
    }
    dependencies {
        classpath "com.linkedin.pygradle:pygradle-plugin:0.6.8"
    }
}

dependencies{
    compile "com.linkedin.pygradle:pygradle-plugin:0.6.8"
}